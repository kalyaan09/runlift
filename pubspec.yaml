name: pro
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.7.0
  firebase_auth: ^5.3.2
  cloud_firestore: ^5.4.5
  firebase_storage: ^12.3.5
  image_picker: ^1.1.2
  cupertino_icons: ^1.0.8
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # Add assets for the wallpaper image
  assets:
    - assets/wallpaper/workout.jpg
    - assets/wallpaper/run.jpg
    - assets/wallpaper/runn.jpg
